div(id="#{rule.id}", style="display:none;")
h3 ACL
table
    thead
        tr(class="titles")
            th Subjects
            th Docs
    tbody
        each userid, i in rule.userIDs
            tr
                td #{userid}
        each docid, i in rule.docIDs
            tr
                td #{docid}
