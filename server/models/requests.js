//americano = require('americano');
cozydb = require('cozydb');

module.exports = {
    contacts: {
        all: cozydb.defaultRequests.all
    }
    rule = {
        all: cozydb.defaultRequests.all
    }
};
